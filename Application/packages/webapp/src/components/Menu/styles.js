import { createStyles, makeStyles } from '@material-ui/core/styles';
// fade,
// Theme,
// theme: Theme
export const useStyles = makeStyles(() =>
  createStyles({
    root: {},
    welcome: {},
  })
);
